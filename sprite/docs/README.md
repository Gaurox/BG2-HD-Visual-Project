# Sprite documentation

`archive/` contains the Historical technical runbooks retained for traceability. Their workspace
paths have been redirected to the current layout; dated measurements and ingame statuses remain
historical.
For a current physical location, resolve `sprite/index/sprite-layout.json` first, then use
`sprite/index/path-migrations.json` only for an immutable legacy reference.

Historical operations entry points: `sprite/README.md`, `sprite/FOLDER_LAYOUT.md`,
`sprite/index/README.md`, and `sprite/docs/archive/SPRITE_FAMILY_CATALOG_APPEND_PIPELINE.md`.
