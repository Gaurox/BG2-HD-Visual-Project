name: Redirect GitHub Pages to project website

on:
  push:
    branches: [main]
    paths:
      - "pages-redirect/**"
      - ".github/workflows/website-pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true
      - name: Upload redirect page
        uses: actions/upload-pages-artifact@v4
        with:
          path: pages-redirect
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
